* Install spacy using conda or python
  ```
  conda install spacy or pip3 install spacy
  ``` 
  </br>
* Then download the english language using 
  ```
  python -m spacy downlaod en_core_web_sm
  ```
  